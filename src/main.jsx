import React from 'react';
import ReactDOM from 'react-dom/client';
import PricingPackageBuilder from '../PricingPackageBuilder.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PricingPackageBuilder />
  </React.StrictMode>
);
